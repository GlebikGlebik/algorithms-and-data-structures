class Queue:
   def __init__(self):
       self.queue = []

   def pop(self):
       if len(self.queue) == 0:
           return None
       res = self.queue.pop(0)
       return res

   def push(self, item):
       self.queue.append(item)